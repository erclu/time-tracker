# clone existing project to current dir

cd to project folder and run: 
`clasp clone <scriptId | scriptURL> [versionNumber]`

after that, push/pull changes

<!-- TODO: set hooks to push/pull from clasp? -->